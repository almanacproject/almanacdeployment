Docker-Compose Examples:

// Start a docker session with VL and Mosquitto.
// Special configuration can be done in the develop.yml file
docker-compose -f docker-compose.yml -p almanac-lab up

// Tidy up images
docker-compose -f docker-compose.yml -p almanac-lab rm
